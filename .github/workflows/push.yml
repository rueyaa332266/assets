name: build
on:
  pull_request

jobs:
  test:
    name: Test2
    runs-on: ubuntu-latest
    steps:
    - name: test1
      run: |
        curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/rueyaa332266/assets/issues/1/labels \
          -d '{"labels":["test2"]}'